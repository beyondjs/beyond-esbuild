# Continuation response: esbuild packaging and Packages

Continue the esbuild work and prepare a bounded integration with Packages. Preserve the completed compiler changes and their recorded evidence. Inspect the current Git state before editing: the pasted report predates the later local commits. No additional commit, push or publication is authorized by this response.

Read the repository instructions and maintained guides, including [execution modes](../docs/execution-modes.md), before proceeding. The primary objective is modular packaging of public packages, including third-party packages such as React, for production distribution and development use. Development HMR remains a separate integration requirement; do not make completing an entire new HMR runtime a prerequisite for reporting a bounded packaging delivery. Internal source files are compilation inputs; packaging must not require the per-file creators/internal runtime objects of the Kernel/Local composition model. Preserve Beyond public module boundaries, exports and bare dependencies.

The existing creator tests are useful compatibility evidence for a legitimate secondary use of esbuild. An independent audit is evaluating the comparative value of granular Kernel HMR; this continuation does not presume that approach should be removed or expanded. They do not demonstrate HMR for esbuild-packaged modules. First establish which output contract the current authored path implements and what remains necessary for the packaging objective. Keep the opt-in assigned-export change, but assess its necessity separately for each path; do not assume that a Kernel requirement applies to every packaged artifact.

For re-exports, reproduce the intended behavior in the packaged path through an independent consuming public module. Distinguish ordinary export correctness from propagation during a development update. Correct the responsible layer based on that evidence. The legacy creator replacement failure alone does not justify implementing the configurable-accessor proposal as a general esbuild requirement. Report remaining runtime-composition limitations separately.

Integrate the fork into the relevant Packages compilation path in a bounded trial, inspecting existing architecture and instructions first. Verify the actual resolved compiler, generated artifacts and execution through Packages. Validate production distribution and development updates separately; a successful rebuild or manually applied legacy Kernel patch is not full HMR evidence. Preserve dependency graphs, source maps and relevant existing regressions. State any update transport or replacement integration still missing instead of claiming it already works.

Architectural context: development must support selecting the mode per public module, between esbuild packaging and the unified Kernel/Local runtime currently called `local-2026`. Keep this boundary clear and avoid choices that prevent later selection. This continuation does not ask you to implement that runtime, finalize its name or deliver the entire mode selector. It does not promise state-preserving switching of a running module.

Expand package coverage beyond the existing React/Express cases to pinned Vue, Svelte and representative UI controls. Distinguish published JavaScript consumption from framework source compilation and identify any adapters required. Execute real consumers on their appropriate browser/Node targets, including production minification and relevant assets, public subpaths, conditional exports and dependency interoperability. Treat broad compatibility as the goal; report concrete coverage and unsupported cases, not a universal guarantee.

Evaluate ESM as the preferred distribution output while preserving CommonJS input compatibility. CommonJS output deprecation is under discussion, not authorized removal. Keep the existing System.register adapter for compatibility; assess whether any actual requirement justifies native emission before proposing compiler expansion. The closure report should explicitly separate native compiler output, adapter output and unresolved format decisions.

Finish with executed evidence, the actual Packages integration status, remaining esbuild-specific work and any concrete decision that blocks it. Update maintained documentation without presenting legacy-runtime tests as packaged-mode acceptance. Keep compiler changes opt-in, upstream behavior and snapshots intact, and repository content portable.
