# Ready-to-copy implementation request

Start the new implementation task from the Beyond ESBuild repository root (`beyond-esbuild/` inside a suite checkout, or another checkout location chosen by the developer). First inspect the current working tree and read `AGENTS.md`, `docs-temp/handoff.md`, and the maintained `docs/` index, setup, architecture, requirements, compiler audit and validation. The fork and bounded demo are prepared using APIs/adapters, with no compiler-core changes; complete Beyond implementation is still open. Do not recreate the fork or overwrite existing work.

Reproduce the recorded baseline, then complete the remaining confirmed Beyond needs with executable evidence across three independent responsibilities: packaging traditional packages (React/ReactDOM and Express), compiling Beyond modules while preserving real internal creators/public bare identities, and tracing direct/transitive file dependencies into separate public-module and package/version graphs. Preserve actual CJS Node/SSR, ESM and SystemJS browser execution, Express HTTP and modular CSS registration/isolation. Keep the simple navigable React demo working through generated artifacts.

This is implementation work, not ratification that the demo already meets every requirement. Extend cases to expose false positives, reproduce the owner's specific conflicts with evidence, and resolve the confirmed gaps. Start from the failure/adaptation ledger in `docs/validation.md`: missed lexer exports, external SystemJS conversion, immutable getter descriptors and the snapshot bridge's live-binding limits. Do not assume in advance that compiler-core changes are either necessary or unnecessary.

Necessary targeted esbuild compiler changes are authorized when a concrete failing case requires them; use existing APIs or bounded adapters where they suffice. Do not call TypeScript System.register conversion native esbuild support or infer browser success from output text. Address the documented live-binding, graph, lifecycle, map and CSS limits according to confirmed contracts, without inventing production compatibility. Preserve upstream MIT attribution and public boundaries. No commits, pushes, publication or general migration of consumers without explicit authorization.

Keep durable guides/results in `docs/`, fixtures/builds/tests in `beyond/`, and temporary handoffs/plans in `docs-temp/`. Finish with actual commands, results, generated artifacts, remaining limits and updated maintained documentation; never claim a universal guarantee from bounded fixtures.

The preceding preparation closeout had explicit authorization for its local commits only. Inspect those commits and current changes before work; that authorization does not grant future arbitrary commits or any push. Keep all repository content portable, without personal filesystem or private tooling paths.
