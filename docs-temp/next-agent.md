# Ready-to-copy continuation request

Continue Beyond ESBuild from the repository root (`beyond-esbuild/` inside a suite checkout, or another checkout location). First inspect the working tree and read `AGENTS.md`, `docs-temp/handoff.md`, and the maintained `docs/` index, setup, architecture, assigned CommonJS exports, requirements, compiler audit and validation. Two deliveries exist and both are committed locally: the first, then the second as the compiler change followed by its fixtures and guides (one opt-in compiler change, creator contract cases, three graphs, composed maps, stylesheet replacement). Do not recreate or overwrite either.

Reproduce the second-delivery run recorded in `docs/validation.md`: every Go test package with unchanged upstream snapshots, the authored runner, the Node tests and the real-browser verification. Then work only on what the owner selects from the open decisions in the handoff, for example replaceable re-exports in creators, a development transport that applies updates and stylesheet changes, or a Packages integration trial. Each needs a source-backed contract and a failing executable case before an adapter or compiler change, and its own regression coverage.

Keep compiler changes opt-in and marked `Beyond ESBuild`; never refresh an upstream snapshot to pass. Do not call the TypeScript System.register conversion native esbuild support or infer browser success from output text. Preserve upstream MIT attribution and public boundaries. No commits, pushes, publication or general consumer migration without explicit authorization. Keep repository content portable, without personal filesystem or private tooling paths.

Keep durable guides and results in `docs/`, fixtures, builds and tests in `beyond/`, and temporary handoffs in `docs-temp/`. Finish with actual commands, results, generated artifacts, remaining limits and updated maintained documentation; never claim a universal guarantee from bounded fixtures.
